/* import styles from "./Card.module.sass"; */

const Card = () => {
    const textStyles = {
        color: "blue",
        fontSize: "srem",
        textAlign: "center"
    }

    return (
        <div style={textStyles}>
            Soy una Card
        </div>
    );
};

export default Card;
